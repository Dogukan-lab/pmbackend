namespace pmbackend.ErrorTypes
{
    public enum ErrorType
    {
        NO_ERROR,
        USER_NOT_FOUND,
        USERNAME_INVALID_LENGTH,
        VALID_USER,
        UNABLE_TO_REGISTER,
        UNABLE_TO_UPDATE,
        USER_NOT_AUTHENTICATED, 
        USER_DELETED,
        UNPRIVILEGED_ATTEMPT
    }
}
